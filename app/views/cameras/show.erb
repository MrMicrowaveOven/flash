<!DOCTYPE html>
<html lang="en" dir="ltr">
  <head>
    <meta charset="utf-8">
    <title></title>
  </head>
  <body>
    <h1>Camera #<%= @camera.id %></h1>
    <% @pictures.reverse.each do |picture| %>
      <div class='image-date'>
        <%= picture.created_at.to_formatted_s(:rfc822) %>
      </div>
      <%= image_tag(picture.photo_url) %>
    <% end %>
  </body>
</html>
